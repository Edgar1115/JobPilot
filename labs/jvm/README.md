# labs/jvm — JVM 实验

候选实验（Backlog）：

- `gc/` — 大量对象下的 Young GC 观察（jstat / JFR）
- `thread-dump/` — 慢线程 / 任务积压下的 Thread Dump 分析（jstack / Arthas）

实验条件（JDK 版本、堆参数、负载）必须记录，数字不得脱离环境使用（规格书强制规则 20）。
每个实验完成后在 `../../docs/experiments/` 记录实验报告。
