# labs/mysql — MySQL 实验

候选实验（Backlog）：

- `index/` — 索引优化对比（EXPLAIN ANALYZE，10w+ 数据）
- `mvcc/` — MVCC 隔离级别实验
- `deadlock/` — 锁顺序与死锁恢复
- `pagination/` — 深分页 / Cursor Pagination 对比

每个实验完成后在 `../../docs/experiments/` 记录实验报告。
