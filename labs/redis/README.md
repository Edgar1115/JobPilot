# labs/redis — Redis 实验

候选实验（Backlog）：

- `global-id/` — Redis Global ID：时间戳 + 自增序列
- `watchdog/` — Redisson WatchDog：锁续期、可重入、所有权
- `stream/` — Redis Stream：Consumer Group / Pending List（PEL）
- `bitmap/` — 签到类场景
- `hyperloglog/` — UV 统计
- `geo/` — 附近位置检索

每个实验完成后在 `../../docs/experiments/` 记录实验报告。
