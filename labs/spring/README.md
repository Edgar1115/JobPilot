# labs/spring — Spring 原理实验

候选实验（Backlog）：

- `transaction/` — @Transactional 失效场景（自调用、异常类型、代理机制）
- `aop/` — Spring AOP：JDK 动态代理 vs CGLIB
- `bean-lifecycle/` — Bean 生命周期

每个实验完成后在 `../../docs/experiments/` 记录实验报告。
